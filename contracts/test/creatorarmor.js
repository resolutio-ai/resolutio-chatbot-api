import { expect } from "chai";
import hre from "hardhat";

describe("Creator Rmour", () => {
    it("Should Add Creator", async () => {

        const creatorArmour = await ethers.deployContract("creatorsArmour");

    });
    it("Should Allow Create TimeTsamp", () => {

    });
    it("Should Get Timestamps", () => {

    });
})